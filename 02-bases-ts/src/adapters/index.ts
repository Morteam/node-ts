import { buildLogger } from './logger.adapter';
import { getAge } from './get-age.adapter';
import { getUUID } from './get-uuid.adapter';
import { httpClientAdapter } from './http-client.adapter';

export {
    buildLogger,
    getAge,
    getUUID,
    httpClientAdapter
}